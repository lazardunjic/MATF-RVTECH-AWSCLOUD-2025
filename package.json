{
  "name": "charging-map",
  "version": "1.0.0",
  "description": "Serverless EV Charging Map with LocalStack",
  "main": "index.js",
  "scripts": {
    "start": "docker-compose up -d",
    "stop": "docker-compose down",
    "deploy": "npx serverless deploy --stage dev",
    "remove": "npx serverless remove --stage dev",
    "logs": "docker-compose logs -f localstack",
    "info": "npx serverless info --stage dev"
  },
  "keywords": [
    "aws",
    "lambda",
    "serverless",
    "localstack",
    "ev-charging"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "^2.1691.0",
    "dotenv": "^17.2.3"
  },
  "devDependencies": {
    "serverless": "^3.40.0",
    "serverless-localstack": "^1.3.1"
  }
}
